<?xml version="1.0" encoding="UTF-8"?>
<!-- ============================================================================================== -->
<!-- /**                                                                                            -->
<!--  * @file        logback.xml                                                                    -->
<!--  * @module      Thinklab Infrastructure / Hash Service & Forensic Audit                        -->
<!--  *                                                                                             -->
<!--  * @description Ultra-high-throughput, non-blocking asynchronous logging topology.             -->
<!--  *              Hardened for reactive event loops (Project Reactor/Netty) to guarantee zero    -->
<!--  *              thread-blocking behavior under extreme peak loads and latency spikes.          -->
<!--  *                                                                                             -->
<!--  * @architecture ADR-003: Non-Blocking Observability & Telemetry                               -->
<!--  *              - Console outputs use ANSI escape sequences with MDC traceId injection.        -->
<!--  *              - File sinks use strict JSON structuring (LogstashEncoder) for ELK/Datadog.    -->
<!--  *              - Async buffers prevent Disk I/O or stdout congestion from halting the JVM.    -->
<!--  *                                                                                             -->
<!--  * @architecture ADR-008: Reactive MDC Context Propagation Bridge                              -->
<!--  *              - Full forensic telemetry matrix (traceId, clientIp, userAgent) injected       -->
<!--  *                across all reactive boundaries for end-to-end traceability.                  -->
<!--  *                                                                                             -->
<!--  * @maintainer  Thinklab Systems Engineering Team                                              -->
<!--  * @version     3.3.0 (Mission-Critical Zero-Blocking Edition + Forensic Origin Telemetry)     -->
<!--  */                                                                                            -->
<!-- ============================================================================================== -->
<configuration scan="false">

    <!-- ============================================================================================== -->
    <!-- GRACEFUL SHUTDOWN HOOK                                                                         -->
    <!-- Ensures asynchronous queues are flushed to disk prior to JVM termination, preventing         -->
    <!-- "ghost telemetry" (loss of in-flight log events) during redeployments or sudden crashes.       -->
    <!-- ============================================================================================== -->
    <shutdownHook class="ch.qos.logback.core.hook.DelayingShutdownHook"/>

    <!-- ============================================================================================== -->
    <!-- [SYNCHRONOUS] CONSOLE APPENDER (HUMAN-READABLE)                                                -->
    <!-- Physical sink for standard output. Optimized pattern including full forensic telemetry matrix. -->
    <!-- ============================================================================================== -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%cyan(%d{HH:mm:ss.SSS}) %gray([%-15.15thread]) %highlight(%-5level) %magenta(%-30.30logger{29}) %green([trace=%mdc{traceId:-NONE} | ip=%mdc{clientIp:-NONE} | client=%mdc{userAgent:-NONE}]) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ============================================================================================== -->
    <!-- [SYNCHRONOUS] AUDIT FILE APPENDER (ROLLING & STRUCTURED JSON)                                  -->
    <!-- Physical sink for persistent storage with time-based rotation and historical bounds.           -->
    <!-- Emits strictly structured JSON (Logstash format) enriched with domain and service metadata.    -->
    <!-- Note: MDC keys (traceId, clientIp, userAgent) are automatically extracted by LogstashEncoder.  -->
    <!-- ============================================================================================== -->
    <appender name="AUDIT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/audit.log</file>

        <!-- Defers disk writes to the OS page cache, massively improving I/O throughput. -->
        <immediateFlush>false</immediateFlush>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/audit.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Retains 30 days of forensic history before automatic purging -->
            <maxHistory>30</maxHistory>
            <!-- Hard limit to prevent disk exhaustion -->
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"thinklab-hash-token-registry-service","domain":"forensic-audit"}</customFields>
        </encoder>
    </appender>

    <!-- ============================================================================================== -->
    <!-- [ASYNCHRONOUS] MISSION-CRITICAL CONSOLE WRAPPER                                                -->
    <!-- Decouples logging from the main reactive event loop. Utilizes non-blocking push semantics.     -->
    <!-- ============================================================================================== -->
    <appender name="ASYNC_STDOUT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="STDOUT" />

        <!-- Expanded queue size to absorb burst traffic safely (Power of 2 alignment). -->
        <queueSize>2048</queueSize>

        <!-- Reserves the last ~20% of the queue (400 items) exclusively for WARN and ERROR events. -->
        <!-- Drops TRACE, DEBUG, and INFO if the queue reaches this limit during extreme traffic.   -->
        <discardingThreshold>400</discardingThreshold>

        <!-- Bypasses expensive stack trace generation for standard logs to save CPU cycles.        -->
        <includeCallerData>false</includeCallerData>

        <!-- NASA-Grade Protection: NEVER block application threads if the queue saturates.         -->
        <!-- If the queue fills up, logs are dropped, but the reactive application survives.        -->
        <neverBlock>true</neverBlock>

        <!-- Forces periodic queue flushing (max 1000ms) to prevent stale telemetry on shutdown.    -->
        <maxFlushTime>1000</maxFlushTime>
    </appender>

    <!-- ============================================================================================== -->
    <!-- [ASYNCHRONOUS] MISSION-CRITICAL AUDIT FILE WRAPPER                                             -->
    <!-- Protects reactive threads from disk I/O bottlenecks and filesystem latency spikes.             -->
    <!-- ============================================================================================== -->
    <appender name="ASYNC_AUDIT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="AUDIT_FILE" />
        <queueSize>2048</queueSize>
        <discardingThreshold>400</discardingThreshold>
        <includeCallerData>false</includeCallerData>
        <neverBlock>true</neverBlock>
        <maxFlushTime>1000</maxFlushTime>
    </appender>

    <!-- ============================================================================================== -->
    <!-- ISOLATED AUDIT LOGGER (STRICT ENCAPSULATION)                                                   -->
    <!-- additivity="false" prevents duplicate event propagation into the root console stream,          -->
    <!-- ensuring forensic logs are solely routed to the JSON file sink.                                -->
    <!-- ============================================================================================== -->
    <logger name="AUDIT_LOG" level="INFO" additivity="false">
        <appender-ref ref="ASYNC_AUDIT"/>
    </logger>

    <!-- ============================================================================================== -->
    <!-- THIRD-PARTY NOISE REDUCTION (SILENCERS)                                                        -->
    <!-- Suppresses verbose discovery/IO chatter from external drivers and reactive networking,         -->
    <!-- keeping the standard output clean and actionable.                                              -->
    <!-- ============================================================================================== -->
    <!-- MongoDB Reactive Driver -->
    <logger name="org.mongodb.driver.cluster" level="WARN"/>
    <logger name="org.mongodb.driver.client" level="WARN"/>
    <logger name="org.mongodb.driver.connection" level="WARN"/>

    <!-- Reactive Networking (Netty/Project Reactor) -->
    <logger name="io.netty" level="WARN"/>
    <logger name="reactor.netty" level="WARN"/>

    <!-- ============================================================================================== -->
    <!-- ROOT LOGGER SINK                                                                               -->
    <!-- Global fallback routing for all framework, library, and application events.                    -->
    <!-- ============================================================================================== -->
    <root level="INFO">
        <appender-ref ref="ASYNC_STDOUT"/>
    </root>

</configuration>